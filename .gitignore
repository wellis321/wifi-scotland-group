config.php
.env
/vendor/
.venv/
.DS_Store
overview.txt
/data/councillors-roster.csv
/data/council-ceo-roster.csv
/data/council-leader-names.csv
/data/council-leader-roster.csv
/data/msp-roster.csv
/data/stakeholder-contacts.csv
/data/mp-roster.csv

# /files/ is the admin-managed public downloads folder (admin/files.php) — it only ever
# lists flat top-level files, never subdirectories, so anything dropped in a subfolder
# (e.g. a Google Search Console export) was never actually served and doesn't belong here.
/files/*.csv
/files/*/
